<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/ic_splash_blank</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

   <!-- SplashActivity theme: AppCompat parent so we can show a layout -->
  <style name="Theme.SleepScan.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- plain solid while system splash is up -->
    <item name="android:windowBackground">@android:color/black</item>
  </style>



</resources>
